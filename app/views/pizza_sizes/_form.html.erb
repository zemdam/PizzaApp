
<%= simple_form_for(@pizza_size) do |f| %>
  <%= errors_for(@pizza_size) %>

  <div class="form-inputs">
    <%= f.input :name %>
    <%= f.input :p_size %>
    <%= f.input :basic_price %>
  </div>

  <div class="form-actions">
    <%= f.button :submit, class: 'btn-primary' %>
  </div>
<% end %>
